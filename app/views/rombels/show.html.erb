<div class="container">
  <div class="card">

    <div class="card-header" align="center">
      <h1>Show Rombel</h1>
    </div>

    <div class="card-body">
      <p>
        <strong>Jurusan:</strong>
        <%= @rombel.jurusan.jurusan %>
      </p>
      <p>
        <strong>Rombel:</strong>
        <%= @rombel.rombel %>
      </p>
    </div>

    <div class="card-footer" align="center">
      <%= link_to 'Edit', edit_rombel_path(@rombel), class: 'btn btn-outline-warning' %>
      <%= link_to 'Back', rombels_path, class: 'btn btn-outline-secondary' %>
    </div>
    
  </div>
</div>